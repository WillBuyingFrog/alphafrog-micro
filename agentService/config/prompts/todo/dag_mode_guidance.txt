只有在以下情况才使用 DAG 模式（通过 dependsOn / parallelizable 明确表达）：
1. 任务可拆分为多个相对独立的子任务，且并行执行可以显著降低总耗时
2. 存在明确的分支或汇聚关系（例如一条上游结果被多个下游消费，或多个上游结果在下游汇总）
3. 问题复杂度足以覆盖 DAG 调度与同步开销

如果任务本质是单链条顺序推进（A -> B -> C），请保持 LINEAR 思路，不要为了并行而并行。
