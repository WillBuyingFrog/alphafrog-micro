# Spring Boot application properties for portfolioService

# Server port (optional, defaults to 8080)
# server.port=8081

# Database configuration (example for H2, replace with your PostgreSQL settings)
# spring.datasource.url=jdbc:h2:mem:portfoliodb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# JPA properties
# spring.jpa.hibernate.ddl-auto=update # Be careful with this in production (options: none, validate, update, create, create-drop)
# spring.jpa.show-sql=true 