<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="world.willfrog.alphafrogmicro.portfolioservice.mapper.HoldingMapper">

    <resultMap id="HoldingResultMap" type="world.willfrog.alphafrogmicro.portfolioservice.domain.PortfolioHoldingPo">
        <id property="id" column="id"/>
        <result property="portfolioId" column="portfolio_id"/>
        <result property="userId" column="user_id"/>
        <result property="symbol" column="symbol"/>
        <result property="symbolType" column="symbol_type"/>
        <result property="exchange" column="exchange"/>
        <result property="positionSide" column="position_side"/>
        <result property="quantity" column="quantity"/>
        <result property="avgCost" column="avg_cost"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="extJson" column="ext_json"/>
    </resultMap>

    <delete id="deleteByPortfolioId">
        DELETE FROM alphafrog_portfolio_holding
        WHERE portfolio_id = #{portfolioId}
          AND user_id = #{userId}
    </delete>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO alphafrog_portfolio_holding
        (portfolio_id, user_id, symbol, symbol_type, exchange, position_side, quantity, avg_cost, ext)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.portfolioId}, #{item.userId}, #{item.symbol}, #{item.symbolType}, #{item.exchange},
             #{item.positionSide}, #{item.quantity}, #{item.avgCost}, CAST(#{item.extJson} AS jsonb))
        </foreach>
    </insert>

    <select id="listByPortfolio" resultMap="HoldingResultMap">
        SELECT id,
               portfolio_id,
               user_id,
               symbol,
               symbol_type,
               exchange,
               position_side,
               quantity,
               avg_cost,
               created_at,
               updated_at,
               ext::text AS ext_json
        FROM alphafrog_portfolio_holding
        WHERE portfolio_id = #{portfolioId}
          AND user_id = #{userId}
        ORDER BY updated_at DESC, id DESC
    </select>
</mapper>
